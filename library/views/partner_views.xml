<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_form_inherit_library" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.library</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page name='library' string="Library"
                               attrs="{'invisible': [('rental_ids', '=', []),
                                                     ('payment_ids', '=', [])]}">
                    <group>
                        <field name="rental_ids"/>
                        <field name="payment_ids" invisible="1"/>
                        <field name="owed_amount"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    <record id="view_partner_filter_inherit_library" model="ir.ui.view">
        <field name="name">res.partner.filter.inherit.library</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <field name="category_id" position="after">
                <field name="owed_amount"/>
            </field>
        </field>
    </record>

    <record id="partner_action" model="ir.actions.act_window">
        <field name="name">Partners</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="view_partner_form_inherit_library"/>
        <field name="search_view_id" ref="view_partner_filter_inherit_library"/>
    </record>
</odoo>
